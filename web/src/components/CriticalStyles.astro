<style is:inline is:global>
  :root {
    --bg: #1a202c;
    --text: #f7fafc;
    --font:
      system-ui, -apple-system, "BlinkMacSystemFont", "Roboto", Helvetica,
      Arial, sans-serif;
  }

  html {
    line-height: 1.4;
    font-family: var(--font);
    background-color: var(--bg);
    color: var(--text);
  }
</style>
